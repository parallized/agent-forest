interface:
  display_name: "Agent Forest"
  short_description: "Run 4-32 parallel specialist agents"
  default_prompt: "Use $agent-forest to plan 4-32 agents, try a live run immediately with the configured API, keep any run-status statements aligned with the latest terminal output, and prefer inline payload input via --payload-stdin or --payload-json instead of creating temp files. Only stop to request missing config if the executor returns a concrete error. Synthesize the final answer locally."

policy:
  allow_implicit_invocation: false
